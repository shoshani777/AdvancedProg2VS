@model IEnumerable<AdvancedProgramming2Server.Models.Chat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
